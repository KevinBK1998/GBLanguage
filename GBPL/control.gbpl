{
    read(a);
    read(b);
    read(c);
    if (a < b) {
        if (b < c)
            write(c); 
        else 
            write(b);
    }
    else {
        if (a < c) write(c);
        else write(a);
    }
}
